# URL Strategy

Use lowercase, slash-terminated directory URLs for static pages. Preserve legacy DNN URLs through 301 redirects.

High-value legacy URLs should redirect to the matching static path. Standalone `/Portals/0/` campaign pages should either be preserved or redirected to the new canonical page depending on launch strategy.
