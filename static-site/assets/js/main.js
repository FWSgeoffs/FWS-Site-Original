const navWrap=document.querySelector('.nav');const btn=document.querySelector('.menu-toggle');if(btn&&navWrap){btn.addEventListener('click',()=>{const open=navWrap.classList.toggle('open');btn.setAttribute('aria-expanded',String(open));});}document.querySelectorAll('form[data-integration-required="true"]').forEach(form=>{form.addEventListener('submit',e=>{e.preventDefault();alert('This form needs to be connected to the approved FutureWork Systems form handler before launch.');});});
